# Roadmaps

High-level index of banlieue's roadmap documents. Full detail for each
phase/initiative lives in [`.github/community/`](.github/community/) — this
file tracks what each one is and its current completion status; the
detailed task lists and design rationale live in the linked doc itself.

Per [`.claude/rules/architecture-driven-development.md`](.claude/rules/architecture-driven-development.md),
architecturally significant work in any roadmap below still goes
**ADR → CALM → TDD → implement → docs → threat model**, in that order — a
roadmap entry describes *what* and *why*, it does not skip the ADR for *how*,
and an implemented ADR is not done until the threat-model pass
([`.claude/rules/threat-modeling.md`](.claude/rules/threat-modeling.md)) has run.

## Status legend

| Symbol | Meaning |
|---|---|
| ✅ | Done — implemented, tested, in the codebase today |
| 🔶 | In progress — some of it exists, not complete |
| ⛔ | Not started |
| 📄 | Reference doc — not a phase with a completion state (principles/conventions) |

## Index

| # | Roadmap | Status | Notes |
|---|---|---|---|
| [00](.github/community/00-overview.md) | Overview | 📄 | Read this first — project shape, principles, phase map |
| [01](.github/community/01-decisions.md) | Decisions | 📄 | The **pre-ADR** decision log (D-001…D-023), annotated 2026-09-19 with the ADRs that superseded individual entries. `docs/adr/` is now canonical; a new decision goes there |
| [02](.github/community/02-conventions.md) | Conventions | 📄 | Code style, error handling, testing, observability |
| [03](.github/community/03-availability-zones-and-datastore-tiering.md) | Availability zones & datastore tiering | ✅ | Failure-domain mapping implemented in `banlieue-provider-vsphere` (verified live against a real vCenter — 3 failure domains reported). Principle locked as **D-023** in [01-DECISIONS](.github/community/01-decisions.md) (2026-09-19); per-zone capability targets are ADR-0030 |
| [04](.github/community/04-phase-1a-controller-and-sdk.md) | Phase 1A — Controller + SDK | ✅ | `banlieue-controller` + `banlieue-provider-sdk` crates exist and are the foundation every provider builds on. Task list reconciled with the tree 2026-09-24 — all 23 boxes were stale-unticked and are now ticked with the file that satisfies each. Three shapes differ from the plan: `main.rs` is `app.rs` under the single binary (ADR-0004), `image_watcher.rs` landed as `reconciler/vmimage.rs`, and the VM → infra-CR integration test is `live_claim.rs` + `e2e_pool_claim.rs` against real infrastructure rather than fake failure domains |
| [05](.github/community/05-phase-1b-vsphere-provider.md) | Phase 1B — vSphere provider | ✅ | `banlieue-provider-vsphere` — the most mature provider; validated end-to-end against real vCenter/ESXi infrastructure. Task list reconciled 2026-09-24: the four planned `client/*` files landed as one `client/vim.rs` behind a trait (`client/fake.rs` is its double), `customize.rs` became `guest.rs` + the SDK's `guestdata.rs`, and the two vcsim test items are **superseded** — `vim_rs`'s `vcsim_compat` needs its SOAP feature, which production does not use, so the live harness (`make vsphere-live-test`) is the real gate. One box still open: the IPAM claim/wait helper, deferred with roadmap 13 |
| [06](.github/community/06-phase-1c-proxmox-provider.md) | Phase 1C — Proxmox provider | ⛔ | No `banlieue-provider-proxmox` crate exists yet |
| [07](.github/community/07-phase-1d-libvirt-provider.md) | Phase 1D — Libvirt provider | ✅ | `banlieue-libvirt` is a first-party pure-Rust native-RPC client (ADR-0011); `LibvirtMachine` lifecycle (ADR-0050) and the NoCloud cloud-init seed (ADR-0054) are implemented and validated end-to-end against a real cluster and libvirt host — a `VirtualMachine` with `spec.userData` becomes a booted domain that receives it, and deleting it removes domain, disks and seed. Install media is ejected once the guest reports installed (ADR-0044, verified live 2026-09-23). Its task list was reconciled with the tree on the same date — eight checkboxes were stale, two of them superseded rather than done (the `bookworm-slim`/`genisoimage` Dockerfile is distroless; `qemu+ssh` is ruled out by ADR-0011's mTLS-only transport). Still open there: `list_all_domains` (a constant, unused) and CI integration against a local libvirt. Not in scope here: vTPM/swtpm (roadmap 17) and pool-based IPAM (roadmap 13) |
| [08](.github/community/08-phase-1e-docs.md) | Phase 1E — Docs site | 🔶 | MkDocs Material site scaffolded, building and deploying to Pages (`docs/`, `docs/site/`); ongoing content work. Task list reconciled 2026-09-24 — 18 of its 19 boxes were done but unticked. The IA diverged from the plan: no `installation/`, `operations/`, `advanced/` or `development/` sections; what exists is Guides (16 pages), Why banlieue? (`reasoning/`, unplanned), Concepts, Architecture, Developer, Security, Reference — read `docs/mkdocs.yml`'s `nav`, not the roadmap. Still open: a changelog page (and whether the internal `.claude/CHANGELOG.md` should be published at all), a contributing page, and `strict: true` for link checking |
| [09](.github/community/09-phase-1f-cloud-hypervisor-provider.md) | Phase 1F — Cloud Hypervisor provider | 🔶 | **First-class, host-resident provider** — not libvirt's `ch` driver (gate decided 2026-09-25, [ADR-0060](docs/adr/0060-cloud-hypervisor-first-class-provider-topology.md), Proposed: `ProviderClass.spec.deployment: External`, self-renewing bound token, no Secret reads). ADR-0061 to ADR-0065 drafted as Proposed the same day: VMM client, `CloudHypervisorMachine`, systemd supervision, OCI artifact delivery, vTPM and `Deferred`. Phase 0 native spike under way: a Kairos `cloudImage` boots on Cloud Hypervisor v53.0 and applies a banlieue-built NoCloud seed from a virtio disk (after a seed-writer date fix). swtpm, TPM-sealed `Deferred` install, installer hot-unplug and the UEFI-variable check passed the same day; only the QEMU timing comparison is still open. **Code started 2026-09-26:** `CloudHypervisorMachine` API and CRDs, the controller's dispatch arm, and the `banlieue-cloud-hypervisor` VMM client (live-tested against v53.0). The host-resident provider is next. Reserves ADR-0060 to ADR-0066. First consumer is roadmap 17 |
| [10](.github/community/10-phase-2-snapshots.md) | Phase 2 — Snapshots | ⛔ | No `VirtualMachineSnapshot`/`SnapshotSchedule` CRDs yet |
| [11](.github/community/11-phase-3-provider-lifecycle.md) | Phase 3 — Provider lifecycle | 🔶 | `ProviderClass` CRD exists and `banlieue-operator` reconciles it into workloads (ADR-0012); deployment topology settled by ADR-0003, namespace isolation by ADR-0016. **Maturity assessed 2026-09-24: 12 of 13 boxes done.** Install/upgrade/prune/pause/cross-namespace are all covered by `make kind-e2e-*` suites; lifecycle lives in `banlieue-operator`, not the main controller; the admission-webhook item is superseded by ADR-0007's `ValidatingAdmissionPolicy`, and `--auto-install` by ADR-0013's `banlieue bootstrap`. The one thing keeping this 🔶 is the Helm chart (roadmap 12 §4.6) |
| [12](.github/community/12-phase-4-finos-ready.md) | Phase 4 — FINOS-ready | 🔶 | Landed: `SECURITY.md`, **55 ADRs** (0001–0055; duplicate numbers resolved 2026-09-19 — 0041/0042 collisions renumbered to 0051/0052), a published [threat model](docs/src/security/threat-model.md), admission policies (ADR-0007), signed/SBOM'd release pipeline (ADR-0006), `kind` E2E (ADR-0014). Task list reconciled 2026-09-24 — the whole of §4.4 security hardening except NetworkPolicy and Secret-rotation-on-watch, §4.7 container images, JSON logging and branch protection were all done but unticked; trivy is superseded by grype + OpenVEX, and `/e2e/` by per-crate `e2e_*.rs` (ADR-0014). Outstanding: `GOVERNANCE.md`/`CODE_OF_CONDUCT.md`/`CONTRIBUTING.md`/`MAINTAINERS.md`/`NOTICE`, Helm chart, CAPI `clusterctl` integration, metrics + tracing (the `--metrics-port` flag reserves a port nothing serves), health endpoints that reflect leader-election state, and NetworkPolicy templates |
| [13](.github/community/13-ipam-pool-integration.md) | IPAM pool integration (ADR-0033, ADR-0053) | ⛔ | Still deferred — needs a decision from the existing IPAM system's owning team on *which* CAPI IPAM provider before any code. Design gap now closed on banlieue's side: ADR-0033 covers `VirtualMachine`, ADR-0053 covers `VirtualMachinePool.spec.addressing` (pool stamps a `poolRef`, members own the claims) |
| [14](.github/community/14-live-migration.md) | Live migration (ADR-0036) | ⛔ | Only the `Recreate`-only placeholder exists today (`crates/banlieue-controller/src/reconciler/migration.rs`); graceful live migration itself not started |
| [15](.github/community/15-vsphere-disk-image-import.md) | vSphere disk-image import (raw/VMDK) | ⛔ | Not started. Closes the follow-up ADR-0010 left open: import the `cloudImage` raw artifact per zone as a VMDK (NFC lease, flat upload as fallback) with a first-party VMDK writer, no install boot in the import Job. ISO path stays, and stays mandatory for `installMode: Deferred`. Reserves ADR-0057 to ADR-0059 |
| [16](.github/community/16-scorecard-remediation.md) | OSSF Scorecard remediation | 🔶 | Vulnerabilities fixed (2026-09-07); **Branch-Protection configured 2026-09-19** — ruleset `main` active with six required checks, which also unblocked Dependabot auto-merge. Code-Review / CII-Best-Practices still need a second reviewer or registration; Pinned-Dependencies capped at 9 by the SLSA generator's tag-only ref. **Aggregator gate landed** (roadmap 16 #1) — `build.yaml` lost its workflow-level `paths:` filter and gained `changes` + `✅ Required Checks`, so docs-only PRs no longer block. ⚠️ Needs a one-time ruleset edit to require the single new context instead of the four |
| [17](.github/community/17-ephemeral-vm-pools.md) | Ephemeral single-use VM pools (AI agent sandboxes) | 🔶 | Phase D (`LibvirtMachine`, ADR-0050), B1 `VirtualMachinePool` (ADR-0046) and B2 `VirtualMachineClaim` (ADR-0047) landed and validated e2e against a real libvirt host; A2 `GuestReady` (ADR-0043) is **closed end-to-end on vSphere** (2026-09-23) — a real `Deferred`, `tpmEnabled` VM ran `vmware-rpctool info-set` at boot and `GuestReady=True` showed up correctly, after fixing three code bugs and one deployment gap (the live `VSphereMachine` CRD hadn't been regenerated for the new status field). **`VirtualMachinePool` is now confirmed live on vSphere** (2026-09-23): a 2-member `GuestReady`-gated pool of `Deferred`/`tpmEnabled` VMs went apply→`Warm=True` in 130.3s. `VirtualMachineClaim` itself is not yet tested against a vSphere-backed pool — its bind/hold/release mechanics are backend-neutral and need no new code to work there (they don't touch the provider at all, and per ADR-0047 Decision 9 need no subject token — the EntraID gap blocking claim testing belongs to the later, unbuilt `AgentSandbox` broker/attestation layer, roadmap phase C, not to the claim itself), but that is an expectation, not yet a live result. **A3** (ADR-0048, `tpmEnabled` requires a non-`Immediate` image) and **A4** (ADR-0044, install media ejected before `GuestReady`) landed 2026-09-23 — A4's eject is verified live against a real libvirtd. **A5** (ADR-0045, vTPM EK certificate) **landed 2026-09-23 and is verified live on libvirt** — a `tpmEnabled` domain's certificate is read out of the guest, its subject CN confirmed to be `<domain-name>:<domain-uuid>`, published on the infra CR and mirrored through `VirtualMachine` onto a bound claim (`make libvirt-ek-live-test`). Roadmap 17 phase D's stated design was wrong and is corrected there: swtpm keeps no host-side copy, so the guest exports the certificate and banlieue reads it with ADR-0043's **read-only** path — it never calls `guest-exec`. On vSphere the certificate is read host-side from `VirtualTpm`, retried on the observe path ADR-0043's vSphere transport added, and gates `GuestReady` the same way; that half is code-and-unit-tests, not yet live. That **unblocks phase F**, whose remaining work is `Provider.spec.attestation.ekTrustBundle` and the in-guest agent, not the anchor. The `ValidatingAdmissionPolicy` pinning a claim's `subject` to its caller (ADR-0047 Decision 10) **landed** — `deploy/admission/virtualmachineclaim-subject-authorization.yaml`, modelled as TB-7 in the threat model. libvirt's own `GuestReady` read path is verified against a real `qemu-guest-agent`, but still needs a Kairos image proving the marker is written at the right *moment*. Open: a live vSphere claim-bind test, that libvirt image gap, `AgentSandbox` (ADR-0055, designed not built), and the section-0 slim-image experiment |
| [18](.github/community/18-split-image-fast-clone.md) | Split-image fast clone — verified base + per-VM sealed volume | ⛔ | Not started. Replaces the per-clone Deferred install (~120s of roadmap 17's 130.3s pool warm-up) with a fast disk clone of an immutable dm-verity base plus an empty per-VM LUKS volume sealed at first boot to that member's own fresh vTPM. Keeps per-member encryption isolation and the ADR-0045/0049 attestation chain; reaffirms ADR-0052 (no memory forking). Phase 0 is the ADR-0067 image-contract gate (amends ADR-0040/0048). Reserves ADR-0067 to ADR-0072. libvirt first, then vSphere (needs roadmap 15's import); Proxmox/Cloud Hypervisor phases land with roadmaps 06/09 |

## Components tracked by ADR, not by a roadmap

These crates grew out of ADRs rather than a numbered phase, so they have no row
above. Their decision record is `docs/adr/`, and that is intentional — ADD
makes the ADR the canonical record, and not every component needs a roadmap:

| Crate | Record |
|---|---|
| `banlieue` (the single binary) | [ADR-0004](docs/adr/0004-single-binary-subcommand-dispatch.md) |
| `banlieue-operator` | [ADR-0012](docs/adr/0012-providerclass-crd-and-operator-role.md), [ADR-0003](docs/adr/0003-provider-deployment-topology.md) |
| `banlieue-imagebuilder` | [ADR-0010](docs/adr/0010-vmimage-build-pipeline-imagebuilder.md), [ADR-0016](docs/adr/0016-imagebuild-namespace-isolation.md) |
| `banlieue-libvirt` | [ADR-0011](docs/adr/0011-libvirt-provider-own-client.md), [ADR-0050](docs/adr/0050-libvirtmachine-domain-lifecycle.md) |
| `banlieue-vex` | supply-chain tooling; see [ADR-0006](docs/adr/0006-release-and-supply-chain-pipeline.md) |

Add a roadmap doc only when a component has multi-phase work to sequence.
A single decision belongs in an ADR.

## Keeping this current

When a roadmap item's status changes (something lands, something new
starts), update its row here in the same PR/commit that makes the change —
this file is a status board, not documentation of intent. Detailed
task-level tracking stays inside each roadmap doc; this file only tracks
the phase-level state.
